# config/genesis

Genesis artifacts for cold-starting `agent_ecology2`.

Treat this subtree as unprivileged bootstrapping content, not kernel policy.
Agent seeds and kernel bootstrap material here should stay explicit about what
is merely a starting convenience versus a core invariant.
