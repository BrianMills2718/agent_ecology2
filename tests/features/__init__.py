"""Per-feature E2E tests that map directly to acceptance criteria.

Each test file corresponds to a feature definition in features/*.yaml.
Tests are named to match acceptance criteria IDs (e.g., test_ac_1_*).

This enables verification that features satisfy their locked specs.
"""
